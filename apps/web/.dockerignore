node_modules
dist
data
.env
.env.*
!.env.example
.git
.gitignore
*.log
npm-debug.log*
.DS_Store
.vscode
.idea
.claude
.agents
.logs

# Personal/deployment runtime config — never bake it into the published image.
# Even a local `docker build` must stay clean; the app falls back to an
# in-bundle mock when this file is absent. The example placeholder is kept.
public/deck-config.json

Dockerfile
.dockerignore
